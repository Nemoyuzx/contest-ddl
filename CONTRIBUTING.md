# 贡献指南

## 新增/更正事件

推荐先提交 Issue。直接 PR 时：

1. 在 `data/manual.yml` 添加官方已发布的事件，或在 `data/overrides.yml` 按现有 ID 更正字段。
2. 每个日期必须能在 `source_url` 指向的主办方、会议、期刊/出版社、承办高校或官方报名系统页面中找到。
3. 使用完整 ISO 8601 时区，例如 `2026-09-01T23:59:59+08:00`。
4. 不把校内提前截止写成全国截止；校内 DDL 请在标题/备注明确学校。
5. 运行 `python -m contestddl update` 和 `python -m pytest`。

可接受来源：主办方官网、会议官网、期刊/出版社官方 CFP、学校官网、官方公众号原文、官方报名平台。搜索结果摘要、群聊截图、无出处表格、往年经验不能单独支持日期。

## 新数据源

适配器放在 `src/contestddl/sources/`，只负责读取并返回 `SourceResult`，不能直接写产物。必须：

- 有公开且可追溯的来源 URL；
- 设置合理超时、重试和请求限速；
- 不绕过登录、验证码、robots 或访问控制；
- 缺失字段返回 `None`，不能猜测；
- 添加离线 fixture 单元测试；
- 在 `docs/sources.md` 记录字段权威性和失效方式。
